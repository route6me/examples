services:
  route6:
    container_name: route6me
    image: route6me/netid:latest
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    cap_add:
      - NET_ADMIN
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=0
    restart: unless-stopped
